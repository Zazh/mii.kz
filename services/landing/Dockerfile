# Используем базовый образ Nginx
FROM nginx:alpine

# Копируем файлы сайта в директорию, которую обслуживает Nginx
COPY . /usr/share/nginx/html

# Nginx автоматически запускается при старте контейнера, конфиг Nginx уже настроен для обслуживания статических файлов
